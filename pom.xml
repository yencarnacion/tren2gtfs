<?xml version="1.0" encoding="UTF-8"?>
<!--
    Generated from archetype; please customize.
-->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>tren2gtfs</groupId>
    <artifactId>tren2gtfs</artifactId>
    <name>tren2gtfs Project</name>
    <version>1.0-SNAPSHOT</version>

    <properties>
        <!--<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>-->
        <!--<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>-->

        <!--<maven.version>3.0.4</maven.version>-->
        <!--<tycho.version>0.16.0</tycho.version>-->
        <!--<aether.version>1.13.1</aether.version>-->
        <!--<wagon.version>2.2</wagon.version>-->
        <gmaven.version>1.5</gmaven.version>
        <!--<gmaven.provider.selection>2.0</gmaven.provider.selection>-->
        <groovy.version>2.0.0-rc-4</groovy.version>
        <gmaven.provider>2.0</gmaven.provider>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.codehaus.gmaven.runtime</groupId>
            <artifactId>gmaven-runtime-2.0</artifactId>
            <version>${gmaven.version}</version>
        </dependency>

        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>3.8.1</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>joda-time</groupId>
            <artifactId>joda-time</artifactId>
            <version>2.2</version>
        </dependency>

        <dependency>
            <groupId>org.codehaus.groovy</groupId>
            <artifactId>groovy-all</artifactId>
            <version>${groovy.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.codehaus.gmaven</groupId>
                <artifactId>gmaven-plugin</artifactId>
                <version>${gmaven.version}</version>
                <configuration>
                    <providerSelection>${gmaven.provider}</providerSelection>
                    <properties>
                        <ressourceDirectoryLocation>
                            ${pom.basedir}/src/main/resources/
                        </ressourceDirectoryLocation>
                    </properties>
                    <scriptpath>
                        <element>${pom.basedir}/src/main/groovy/tren2gtfs/</element>
                    </scriptpath>
                    <source>
                        ${pom.basedir}/src/main/groovy/tren2gtfs/tren2gtfs.groovy
                    </source>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>generateStubs</goal>
                            <goal>compile</goal>
                            <goal>generateTestStubs</goal>
                            <goal>testCompile</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
